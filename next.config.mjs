/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["robohash.org", "store.istad.co","cdn.pixabay.com","images.unsplash.com",'fakestoreapi.com'],
    
  },
};

export default nextConfig;
